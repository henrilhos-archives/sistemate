version: '3'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: sistemate
      POSTGRES_USER: sistemate
      POSTGRES_PASSWORD: sistemate
    volumes:
      - pg-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  pg-data:

